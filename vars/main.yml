---
# users to deploy
_udm_users: '{{ udm_users | mandatory }}'

# whether to enable passwordless sudo
_udm_enable_passwordless_sudo: '{{ udm_enable_passwordless_sudo | default(_def_udm_enable_passwordless_sudo) }}'

# whether to quiet assert
_udm_quiet_assert: '{{ udm_quiet_assert | default(_def_udm_quiet_assert) }}'
...
